// Sentinel source name for synthetic events fired from the dashboard
// "send test ping" button. The dashboard filters this value out of analytics
// aggregations but keeps it visible in the Recent events feed so the user
// gets visible confirmation that the pipeline works.
export const TEST_PING_SOURCE = "Test ping";
